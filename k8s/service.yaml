apiVersion: v1
kind: Service
metadata:
  name: flask-myapp-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: flask-myapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080